// Murray's FSM - ESLint Flat Config (Next.js 16)
// ==============================================

import nextCoreWebVitals from 'eslint-config-next/core-web-vitals';

const config = [
  ...nextCoreWebVitals,
];

export default config;
